---
import { config } from "@/config";
const umami = config.site.umami;
const showUmami = umami && umami !== "";
---

{
  showUmami && (
    <script
      is:inline
      defer
      src="https://cloud.umami.is/script.js"
      data-website-id={umami}
    />
  )
}
